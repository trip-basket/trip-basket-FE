# 실시간 공유 편집 기능 요구사항

## 📋 프로젝트 개요

여행 계획을 여러 사람이 동시에 편집할 수 있는 실시간 협업 캘린더 시스템

---

## 🛠 기술 스택

| 구분 | 기술 |
|------|------|
| Frontend | Next.js 16, React 19, TailwindCSS, Google Maps API |
| Backend | Spring (기존) |
| 실시간 통신 | Socket.io (예정) |

---

## 📅 캘린더 UI 구조

### 레이아웃
- **가로축**: 날짜 (여행 기간에 따라 동적)
- **세로축**: 시간 (00:00 ~ 24:00)
- 여행 기간 설정 가능
- 드래그 앤 드롭으로 일정 이동/수정

### 블록 (일정)
- **표시 정보**: 장소명, 시간, 좋아요 수
- **상세 정보**: 블록 클릭 시 장소 상세 정보 표시
- 시작 시간 ~ 종료 시간 형태
- 하루에 여러 일정 추가 가능

---

## 🗺 장소 추가 플로우

### 방법 1: 캘린더에 바로 추가
1. 지도에서 장소 검색/선택
2. 장소 상세 정보 패널에서 "일정에 추가하기" 버튼 클릭
3. Draggable 블록 생성
4. 캘린더의 원하는 위치에 드래그하여 배치

### 방법 2: "가고 싶은 장소" 사이드바에 추가
1. 지도에서 장소 검색/선택
2. "가고 싶은 장소에 추가" 버튼 클릭
3. 사이드바에 블록 저장
4. 나중에 사이드바에서 캘린더로 드래그하여 이동

---

## 👥 실시간 협업 기능

### 동시 편집
- 여러 사람이 동시에 같은 방(Room)에서 편집
- **충돌 해결**: 블록 점유 시 잠금(Lock) 방식
- 실시간 커서 표시: ❌ 불필요

### 동기화
- Socket.io를 통한 실시간 상태 동기화
- 블록 추가/수정/삭제/이동 시 즉시 반영

---

## 📱 UI 구성 요소

### 1. 캘린더 영역 (메인)
- 날짜 x 시간 그리드
- 블록 표시 및 드래그 앤 드롭

### 2. 사이드바 (가고 싶은 장소)
- 저장된 장소 블록 목록
- 캘린더로 드래그 가능

### 3. 지도 영역
- Google Maps 통합 (이미 구현됨)
- 장소 검색/선택 (이미 구현됨)
- 장소 상세 정보 패널 (추가 필요)

### 4. 반응형
- 모바일: 아직 미고려

## 추가 확인 필요 사항

### 시간 충돌 처리
- [ ] 같은 시간대에 여러 블록이 겹칠 수 있나요?
- [ ] 겹치면 어떻게 표시하나요? (나란히? 겹쳐서? 경고?)

### 블록 잠금 상세
- [ ] 잠금 지속 시간은? (편집 중일 때만? 타임아웃?)
- [ ] 잠금된 블록은 어떻게 표시하나요? (색상? 아이콘?)
- [ ] 다른 사용자가 잠금된 블록을 클릭하면 어떻게 되나요?

### 좋아요 기능
- [ ] 참여자들이 각 장소에 투표하는 기능인가요?
- [ ] 한 사람당 한 번만 좋아요 가능한가요?

### 여행 기간 설정
- [ ] 시작일/종료일을 어디서 설정하나요? (별도 UI? 캘린더 내?)
- [ ] 기간 변경 시 기존 일정은 어떻게 되나요?

### 블록 관리
- [ ] 블록 삭제 기능이 필요한가요?
- [ ] 블록 시간 조정 (드래그로 늘리기/줄이기) 가능한가요?
- [ ] 블록 복제 기능이 필요한가요?

### 참여자 관리
- [ ] 현재 접속 중인 참여자 목록 표시가 필요한가요?
- [ ] 참여자 초대/퇴장 기능이 필요한가요?

---

## 📁 현재 프로젝트 구조

```
travel-basket/
├── app/
│   ├── plan/[roomId]/page.tsx    # Room 페이지
│   └── ...
├── src/
│   ├── components/ui/            # UI 컴포넌트 (Button, Input, Text)
│   └── feature/
│       ├── calendar/             # 캘린더 (기본 구조만)
│       ├── maps/                 # Google Maps (장소 검색 구현됨)
│       └── room/                 # Room 콘텐츠 레이아웃
└── ...
```
